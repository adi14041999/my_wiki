[project]
name = "my-wiki"
version = "0.1.0"
description = "Personal wiki (MkDocs)"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "mkdocs>=1.5.0",
    "mkdocs-material>=9.0.0",
    "pymdown-extensions>=10.0",
]

